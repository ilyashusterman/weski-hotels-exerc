export interface QueryBody {
  ski_site: number;
  from_date: string;
  to_date: string;
  group_size: number;
}
export interface QueryBodyRaw {
  ski_site: string;
  from_date: string;
  to_date: string;
  group_size: string;
}
